javascript: 
function findTerm (term) {
	alert(term);
	if (window.find(term))
		{
			// findList.push(term);
			// alert(findList);
		}
}
// var searchList = ["compensation consult", "consult", "towers", "mercer", "radford", "hewitt", "watson"];
// var findList = new Array();
// for (var i in searchList) 
// {
	// findTerm(i);
// }
alert(findTerm(the));
